import { VolunteerFilterPipe } from './volunteer-filter-.pipe';

describe('VolunteerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VolunteerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
